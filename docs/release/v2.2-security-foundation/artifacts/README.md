# Snapshot artifacts

Place Analyzer downloads here on the Windows box. Do not commit files that contain a full employee roster unless the team already treats this folder as UAT-only.

Expected names:

- `snapshot_before_canary.txt`
- `snapshot_after_grant.txt`
- `snapshot_after_rollback.txt`
