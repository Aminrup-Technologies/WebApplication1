# SQL evidence

Catalog / environment:

Scripts run (name, dry-run vs apply):

## Before

```sql

```

Paste SSMS:

```

```

## After

| Field | Expected | Observed |
| --- | --- | --- |
| | | |

Do not write passwords, `LoginStatus`, `LastLogin`, `UserRoleDB`, or `RolePermissionDB` unless the release explicitly requires it.
